## Setup

1. Clone this repo
2. Create a file called `config.js` in the root folder
3. Add your tokens:

const CONFIG = {
  MAPBOX_TOKEN: 'your mapbox pk token',
  INAT_TOKEN:   'your inaturalist jwt token'
};

4. Open index.html with Live Server